import React from 'react';
import ReactDOM from 'react-dom';
import MainApp from './App'; // Import the renamed MainApp component

ReactDOM.render(
  <React.StrictMode>
    <MainApp />
  </React.StrictMode>,
  document.getElementById('root')
);
